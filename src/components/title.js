import react from "react";

const title = () => (
    <div>
        <h1 className="title-container-title">what's the weather like in...</h1>
        <h3 className="title-container-subtitle">Fin out Temperature..</h3>
    </div>
);

export default title;